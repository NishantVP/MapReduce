<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.v1_0.coen317dc.sneha.nishant.www.mapreduce">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MapReduce"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TestingActivity"
            android:label="@string/title_activity_testing"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".FirstConnectionService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".MapService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".ReduceService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".AutoRepeateMapReduceService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>
